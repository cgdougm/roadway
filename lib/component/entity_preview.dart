import 'package:flutter/material.dart';

class EntityPreview extends StatelessWidget {
  const EntityPreview({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}